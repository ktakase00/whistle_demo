<%= form_with(model: milestone_report, local: true) do |form| %>
  <% if milestone_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(milestone_report.errors.count, "error") %> prohibited this milestone_report from being saved:</h2>

      <ul>
      <% milestone_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :milestone_id %>
    <%= form.text_field :milestone_id %>
  </div>

  <div class="field">
    <%= form.label :report_at %>
    <%= form.datetime_select :report_at %>
  </div>

  <div class="field">
    <%= form.label :status_cd %>
    <%= form.text_area :status_cd %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
